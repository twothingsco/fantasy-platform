# apps/dashboard/.env.development
# VITE_ vars are inlined into the browser bundle — anon key only, never service_role.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
VITE_EXPRESS_PORT=3000
