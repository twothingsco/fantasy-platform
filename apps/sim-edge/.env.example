# apps/sim-edge/.env  — used by the Supabase CLI for local `supabase start` / serve.
# For deployed functions these are set with:
#   supabase secrets set --project-ref <ref> KEY=value
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
YAHOO_CLIENT_ID=
YAHOO_CLIENT_SECRET=
YAHOO_REDIRECT_URI=
GROQ_API_KEY=
