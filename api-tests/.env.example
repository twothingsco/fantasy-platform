# Supabase anon keys for the Bruno collection.
# Copy to .env and fill in. Both are anon (public) keys, not service_role.
# Find them in: Supabase dashboard -> Project Settings -> API -> Project API keys -> anon
FANTASY_ANON_KEY=
POINTZONE_ANON_KEY=
